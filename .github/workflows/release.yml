name: release

on:
  push:
    branches:
      - "main"

jobs:
  release:
    permissions:
      contents: write
      pull-requests: write
      id-token: write
    uses: signoz/primus.workflows/.github/workflows/js-release.yaml@js-release
    secrets: inherit
    with:
      PRIMUS_REF: git
